# dependencies
node_modules/

# certificates — never commit private keys or certs
certs/
*.pem
*.key
*.crt
*.p12
*.pfx

# reports/ and collection/live-collection.json are
# committed by GitHub Actions — do NOT ignore them here.
# If you want to keep them local only, uncomment below:
# reports/
# collection/live-collection.json
